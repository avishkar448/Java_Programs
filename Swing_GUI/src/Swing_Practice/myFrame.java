package Swing_Practice;

import javax.swing.*;
import java.awt.*;

public class myFrame {
    public static void main(String[] args) {
       /* JFrame frame=new JFrame(); //create frame
        frame.setTitle("My frame title");//set title
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);//exit out of application
        frame.setResizable(false);
        frame.setSize(420,420);//x-dimension and y-dimension
        frame.setVisible(true);

        ImageIcon image=new ImageIcon("1.png");//create a image icon
        frame.setIconImage(image.getImage());//change icon of frame
       frame.getContentPane().setBackground(new Color(255, 0, 200));//change background color
        */
        new MyFrame1();
    }
}
